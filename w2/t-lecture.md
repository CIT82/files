<h3>Assignment: freeCodeCamp Part 2 - Attributes, Debugging, & Boilerplates</h3>

<h3>Background</h3>
<p>Welcome to Part 2 of your freeCodeCamp journey! In Part 1, you set up your account and learned about the basic building blocks of web pages. Now, we are going to look at how to make those elements more powerful, how to fix broken code, and how to set up the HTML boilerplate "skeleton" that every single website needs.</p>

<h3>Instructions</h3>

<h3>Step 1: Complete the Modules</h3>
<p>Log into freeCodeCamp and complete the following three sections:</p>
<ul>
    <li><strong>Understanding HTML Attributes</strong></li>
    <li><strong>Debug a Pet Adoption Page</strong></li>
    <li><strong>Understanding the HTML Boilerplate</strong></li>
</ul>

<h3>Step 2: Capture and Resize Your Work</h3>
<p>Once you have finished all three sections, take a screenshot of your curriculum dashboard showing the completed checkmarks. Just like we did in Part 1, you need to resize this image before adding it to your project so your website loads quickly!</p>
<p>This work is done in your private repo.</p>
<ul>
    <li>Open your screenshot in an image editor (like Preview on Mac, Paint on Windows, or a free online tool).</li>
    <li>Resize the image so it is a manageable size for the web (around 500pixels wide), like we did for Part 1</li>
    <li>Save this resized image inside your project's <code>img</code> folder as <code>w2p2.png</code> (or whatever naming convention you started using in Part 1).</li>
</ul>

<h3>Step 3: Code Your index.html</h3>
<p>Now, let's practice what you just learned! Open your <code>index.html</code> file in your code editor. We are going to organize your "Proof of Work" links to make them look cleaner using an unordered list (<code>&lt;ul&gt;</code>) and list items (<code>&lt;li&gt;</code>).</p>
<p>Write the following code structure into your page. Make sure to include your link from Part 1, and add your new link for Part 2:</p>

<pre><code>
&lt;ul&gt;
  &lt;li&gt;Week 2 Part 1: &lt;a href="img/w2p1.png"&gt;Proof of Work&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;Week 2 Part 2: &lt;a href="img/w2p2.png"&gt;Proof of Work&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</code></pre>

<h3>Step 4: Push to GitHub</h3>
<p>Open your terminal and use your Git commands (<code>git add .</code>, <code>git commit -m "added w2p2 proof of work list"</code>, and <code>git push</code>) to send your updated <code>index.html</code> and your new resized screenshot up to your repository.</p>